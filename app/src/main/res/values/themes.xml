<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen theme: uses IconBackground parent to support icon circle -->
    <style name="Theme.HerMoodBarometer" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/Theme.HerMoodBarometer.PostSplash</item>
    </style>

    <!-- Post-splash app theme: DayNight with transparent system bars.
         windowBackground must match the app's actual background color so that
         Activity recreation (e.g. locale change) doesn't flash white/black. -->
    <style name="Theme.HerMoodBarometer.PostSplash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
