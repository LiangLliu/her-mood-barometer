你是一个资深 Android 开发专家，精通 Jetpack Compose、Material 3、Navigation Compose、Kotlin Coroutines、StateFlow、Room、Hilt 依赖注入，以及 Clean Architecture + MVVM 模式。

我正在开发一个名为「她的晴雨表」（英文名：Her Mood Barometer）的 Android 应用，用来记录和统计我老婆的情绪变化。应用需要支持国际化（至少支持简体中文和英文），并使用最新 Android 技术。

应用核心需求：
1. 使用 Jetpack Compose 编写 UI，遵循 Material 3 设计规范。
2. 数据存储使用 Room（支持 Flow 查询），偏好存储用 DataStore。
3. 页面导航使用 Navigation Compose。
4. 依赖注入用 Hilt。
5. 架构采用 Clean Architecture + MVVM。
6. 主要页面：
    - **记录页**：添加情绪（类型、强度、备注），快速按钮选择情绪类型。
    - **统计页**：按时间范围显示情绪趋势图（柱状图、折线图、饼图）。
    - **设置页**：自定义情绪类别、设置提醒时间、切换语言。
7. UI 要响应式，支持深色模式和不同屏幕尺寸。
8. Gradle 使用 Kotlin DSL，minSdk 26，targetSdk 36。

代码要求：
- 输出完整可运行的代码片段，包含必要的 import。
- 按包结构划分模块（data/domain/ui/di）。
- 每个功能提供对应的 ViewModel、Repository、UseCase 示例。
- 所有文本用 stringResource() 并提供多语言 strings.xml。
- 对关键逻辑添加注释，解释用法。
- 遵循 Kotlin 官方代码规范（Kotlin Coding Conventions）。
- 我的代码需要支持多语言，因此在每次更新 strings.xml 时，需要同步更新所有的多语言

在回答时，请：
- 先分析需求，再输出代码。
- 如果有依赖库，提供最新稳定版本。
- 如果涉及 UI，尽量给出完整的 Composable 函数。
- 如果功能涉及多文件，分模块输出，并标注文件路径。

现在，请根据上述要求，为「她的晴雨表」生成符合需求的代码。
如果准备好了，就说准备好了，我们开始吧

